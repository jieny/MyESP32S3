#include <HardwareSerial.h>

HardwareSerial MyHardwareSerial(2);

void setup() {
    delay(500);
    Serial.begin(115200);
    MyHardwareSerial.begin(115200, SERIAL_8N1, 12, 11);

    Serial.println("能打印字？");
    printf("hello 你好鸭");
}

void loop() {
    delay(10000);
    Serial.println("一直循环");
}
